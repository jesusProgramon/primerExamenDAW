FROM nginx:alpine

# Instalar herramientas útiles para debugging
RUN apk add --no-cache \
    curl \
    iputils \
    bind-tools

# Exponer puerto
EXPOSE 80

# Comando por defecto
CMD ["nginx", "-g", "daemon off;"]
