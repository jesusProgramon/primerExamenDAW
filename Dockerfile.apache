FROM httpd:latest

# Instalar PHP y herramientas útiles para debugging
RUN apt-get update && apt-get install -y \
    php \
    curl \
    iputils-ping \
    dnsutils \
    && rm -rf /var/lib/apt/lists/*

# Exponer puerto
EXPOSE 80

# Comando por defecto
CMD ["httpd-foreground"]